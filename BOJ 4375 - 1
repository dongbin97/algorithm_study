#include <stdio.h>
using namespace std;

int main() {
	int num;
	while (scanf("%d", &num) != -1) {
		long long one = 1;
		int cnt = 1;
		while (one % num != 0) {
			one = (one * 10 + 1) % num;
			cnt++;
		}
		printf("%d\n", cnt);
	}
	return 0;
}
