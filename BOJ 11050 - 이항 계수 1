#include <stdio.h>

int fac(int n) {
	int res = 1, i=1;
	while (i <= n) {
		res = i * res;
		i++;
	}
	return res;
}
int main() {
	int N, K;
	scanf("%d %d", &N, &K);
	printf("%d", fac(N) / (fac(K) * fac(N - K)));
}
