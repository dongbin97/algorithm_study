#include <stdio.h>

int main() {
	int T, N, M, x, y;
	scanf("%d", &T);
	while (T--) {
		scanf("%d %d %d %d", &N, &M, &x, &y);

		int idx;
		for (idx = x; idx <= N * M; idx += N) {
			if (idx % M == y % M)
				break;
		}
		if (idx > N * M)
			printf("-1\n");
		else printf("%d\n", idx);
	}
}
